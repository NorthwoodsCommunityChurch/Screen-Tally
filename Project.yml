name: ScreenTally
options:
  bundleIdPrefix: com.northwoodschurch
  deploymentTarget:
    macOS: "15.0"
  xcodeVersion: "16.0"
  createIntermediateGroups: true
  defaultConfig: Release

packages:
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle
    from: 2.0.0

settings:
  base:
    SWIFT_VERSION: "6.0"
    MACOSX_DEPLOYMENT_TARGET: "15.0"
    CODE_SIGN_IDENTITY: "-"
    CODE_SIGN_STYLE: Manual

targets:
  ScreenTally:
    type: application
    platform: macOS
    sources:
      - path: ScreenTally
    dependencies:
      - package: Sparkle
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.northwoodschurch.screentally
        PRODUCT_NAME: "Screen Tally"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ENABLE_USER_SCRIPT_SANDBOXING: false
    info:
      path: ScreenTally/Info.plist
      properties:
        CFBundleShortVersionString: "1.0.6"
        CFBundleVersion: "1.0.6"
        CFBundleDisplayName: Screen Tally
        LSUIElement: true
        NSHumanReadableCopyright: "Copyright 2025 Northwoods Community Church"
        SUPublicEDKey: VIMxKZmmRokdMcHK5d3QU4+qHgBglmkVFP5aAVvxgqM=
        SUFeedURL: https://northwoodscommunitychurch.github.io/app-updates/appcast-screentally.xml

schemes:
  ScreenTally:
    build:
      targets:
        ScreenTally: all
    run:
      config: Debug
    archive:
      config: Release
